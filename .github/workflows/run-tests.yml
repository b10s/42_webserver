name: cpp-test

on:
  push:
    branches:
      - "**"
    paths: ["**"]

jobs:
  cpp-test:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v5

      - name: Run make test
        run: |
          make test
